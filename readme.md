## Import Bulk Data (CSV to MongoDB)

### Project config and run

- Python3 install and check version `python3 -V`
- Install pip3: `sudo apt update`, `sudo apt install python3-pip` and check version `pip3 -V`